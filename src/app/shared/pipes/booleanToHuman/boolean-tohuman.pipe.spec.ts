import { BooleanTohumanPipe } from './boolean-tohuman.pipe';

describe('BooleanTohumanPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanTohumanPipe();
    expect(pipe).toBeTruthy();
  });
});
